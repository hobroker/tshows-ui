import React from 'react';
import { Box, Typography } from '@mui/material';

const WelcomeStep = () => (
  <Box>
    <Typography variant="body1">
      This wizard will guide you through the process of setting up your account.
    </Typography>
  </Box>
);

export default WelcomeStep;
